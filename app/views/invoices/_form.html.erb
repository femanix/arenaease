<%= form_with(model: invoice) do |form| %>
  <% if invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
        <% invoice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comanda_id %>
    <%= form.text_field :comanda_id %>
  </div>

  <div class="field">
    <%= form.label :nota_chave %>
    <%= form.text_field :nota_chave %>
  </div>

  <div class="field">
    <%= form.label :nota_numero %>
    <%= form.text_field :nota_numero %>
  </div>

  <div class="field">
    <%= form.label :valor_cents %>
    <%= form.number_field :valor_cents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
