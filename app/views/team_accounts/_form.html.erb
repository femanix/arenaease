<%= form_with(model: team_account) do |form| %>
  <% if team_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_account.errors.count, "error") %> prohibited this team_account from being saved:</h2>

      <ul>
        <% team_account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.text_field :team_id %>
  </div>

  <div class="field">
    <%= form.label :balance_cents %>
    <%= form.number_field :balance_cents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
